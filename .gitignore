# === Rust / Cargo 构建产物 ===
# Cargo 编译输出目录，几十 MB 到 GB 级，绝不上传
/target/

# === runtime 程序本体 ===
# dsh 本体（运行时下载到 dshr/dsh/），发布不带；node portable（未来）也在这
/dsh/

# === 本地配置与临时目录 ===
# 敏感配置（API key 等），绝不提交
.env
.env.*
!.env.example
# 本地配置（决策 12 的 config.json；v3 起 api-key 直接放这，必须忽略）
/config.json
/secrets.json

# 冒烟/测试用工作目录
.tmp-ws/

# 本地数据库与数据文件（数据罗盘 data/ 落盘目录，DESIGN §11.1）
/data/
# sqlite 旁路文件（dshr.db 打开时的临时/回滚/WAL 文件；若库放 data/ 外同样忽略）
*.db-shm
*.db-wal
*.db-journal

# === 编辑器 / IDE ===
.vscode/
.idea/
*.swp
*.swo

# === 系统杂项 ===
.DS_Store
Thumbs.db

# === 备注 ===
# dshr-ui/assets/fonts/ 需要入库（Noto Sans SC 子集 + OFL.txt）：中文字体运行时内置，
# 忽略会导致克隆后无法编译（main.rs include_bytes 引用）。勿加入本文件忽略规则。
